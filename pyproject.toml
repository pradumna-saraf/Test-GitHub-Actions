[project]
version = "0.1.4"
description = "Aviationstack MCP server."
readme = "README.md"

[project.scripts]
aviationstack-mcp = "aviatihgjhonstack_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
